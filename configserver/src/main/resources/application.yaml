spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/varunbansal946/config-repository.git
          default-label: main
          clone-on-start: true
server:
  port: 8888

management:
  endpoints:
    web:
      exposure:
        include: health,info